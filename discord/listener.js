// listen to discord messages from a specific channel as specified in the config file

async function listen(client) {
    if(!client) return;

    // TODO: tell the minecraft bot what to do based on messages in the right channel.
}

client.exports = {
    listen
}